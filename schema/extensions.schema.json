{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://muse.dev/schema/extensions.schema.json",
  "title": "Muse extensions",
  "description": "Namespaced escape hatch (SCHEMA_SPEC.md §2.7). Keys are namespaces (e.g. engine.audiocraft); contents are unrestricted and engine-specific. Conforming engines must ignore unknown namespaces, never fail on them.",
  "type": "object",
  "propertyNames": {
    "type": "string",
    "pattern": "^[a-z0-9]+([._-][a-z0-9]+)*$",
    "description": "Namespace, e.g. engine.audiocraft or composer.my_tool."
  },
  "additionalProperties": true
}
