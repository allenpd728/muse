# Test spec — #114: AGENTS.md Build/test + layout currency

**Source task:** #114 (AGENTS.md stale Build/test + layout)
**Code under test:** `AGENTS.md` — documentation, but the commands it names
are executable claims.

## Behaviors to verify

- **Every command named in Build/test runs:** `npm test`,
  `npm run validate -- tools/fixtures/valid.muse.json`,
  `npm run test:explorer` — a docs-currency check (grep AGENTS.md for the
  `npm` command names, assert each resolves in package.json scripts or is
  the documented `node benchmark/metrics.mjs` CLI).
- **Layout accuracy:** every directory listed in the layout block exists
  on disk; new top-level dirs (e.g. a future `listener/` split) fail the
  check until the layout is updated.

## How to run

`tests/docs-currency.test.mjs` (or fold into `tests/conventions.test.mjs`);
`npm test` picks it up.

---

## Closed — 2026-08-22 (issue #117)

Coverage landed: `tests/docs-currency.test.mjs` — 20 checks:

- Every `npm run <name>` command named in the Build/test section resolves
  as a package.json script (validate, test:explorer, expand, render, play);
  `npm test` named + resolves; the benchmark metrics CLI named + on disk.
- Every directory in the layout block exists on disk — a new top-level dir
  fails the check until the layout is updated.

Run: `npm test` (folds in `tests/docs-currency.test.mjs`).
