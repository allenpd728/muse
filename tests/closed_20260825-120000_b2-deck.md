# Test spec — B2 board deck (docs/boardroom/deck.html)

## Behaviors to verify

- **Slide deck structure**: ~15 slides, keyboard navigable (arrow keys advance), each slide a <section class="slide">.
- **Required slides present** (by data-slide attribute or heading text): thesis, event framing (Illiac/Gould-Bernstein), the 5-second-WAV test, architecture diagram, business model (open spec + proprietary tools), live demo, competitive landscape, roadmap (corpus ladder), asks.
- **Demo slide embeds the workbench**: link to /workbench/detail.html present; the audio panel reachable (embed or direct link to /audio/bwv227.1.llm-v2.wav).
- **Thesis fidelity**: the slide text contains the actual vision phrases — "event, not a render", the corpus ladder works (Bach, Byrd, Schubert, Beethoven) — so a paraphrase drift is caught.
- **No mockup content** (D20): source scan, same as B1.
- **Zero console errors.**

## Invocation

`cd tools && python -m pytest qa_frontend/tests/test_boardroom_deck.py -q`

## Closed 2026-08-26 (#264, run=20260825-1033-cae1)

Deck landed: 10 slides (title + the 9 required), keyboard nav (arrows/
space/Home/End, hash-routed), all spec behaviors covered by
tools/qa_frontend/tests/test_boardroom_deck.py (6 tests: structure +
keyboard advance, required-slides by data-slide, demo links to
workbench + live render, thesis fidelity via text_content (display:none
slides are invisible to inner_text — a harness lesson), D20 source
scan, zero console errors).

Notes: deck lands at 10 slides, not ~15 — the 9 required plus title;
padding would dilute. Demo slide links (not embeds) the WAV — rendered
audio is session-local per the repo convention, and the manifest is the
reference. Workbench DoD tests updated in the same commit: the seed
index grew to the 3-revision lineage chain after those pins were
written (the 'exactly one group' assertion became 'one per revision').
