# Test spec — Batch 1 #7: form.schema.json

**Source task:** #7 (JSON Schema for the `form` block, spec §2.4)
**Code under test:** `schema/form.schema.json`; suite `tests/form.test.mjs`
(runs standalone and under `npm test`).

The task's DoD cases already landed (energy outside 0–1 rejected; repeated ids
in `order` accepted). This spec captures residual and integration coverage.

## Behaviors to verify (already in `tests/form.test.mjs`)

- Spec §2.4 snippet end-to-end valid, including transform-suffixed
  `variation` string.
- `order` with repeated section ids accepted; empty-string id rejected; empty
  `sections` rejected.
- Full `role` enum; unknown role rejected.
- `energy` boundary: exactly 0 and 1 accepted; -0.1 / 1.1 rejected.
- `uses[].ref` required; unknown members rejected
  (`additionalProperties: false` decision).
- `repetition` entries require both `min` and `max`.

## Residual coverage this follow-up must add

- **Root-schema integration:** validate `form` through `schema/muse.schema.json`
  after #11 lands (section schema currently compiled standalone).
- **Semantic checks (code, not ajv — same class as the harness's cross-ref
  lint):** `repetition` min ≤ max; `order` after filtering resolves to defined
  section ids; `repetition` keys are section ids; section ids are unique.
  `form.sections[].uses[].ref` / `harmony` resolution is already covered by the
  harness cross-ref check (#3) once real examples exist.
- **Edge cases:** `repetition` min == max (fixed count — allowed; currently
  not explicitly tested); role `custom` with free organization of sections
  (no other hooks — pin intent);
  `bars` inconsistency vs `order` length — decide whether any semantic relation
  should be linted.

## How to run

`node tests/form.test.mjs` or `npm test`.

## Notes

Implementation calls made on the schema task (retrospective review):
`sections` and `order` required at the top level; `repetition` optional;
`additionalProperties: false` per object; `id`/`ref` strings `minLength: 1`.
Duplicate section ids are not schema-rejected — uniqueness is semantic.
