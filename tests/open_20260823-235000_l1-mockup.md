# Test spec — L1 mockup harness

**Task:** #173 (L1 — Mockup harness)
**Written:** 2026-08-23

## What to verify

1. **Model**
   - Mockup validates if notes present; raises MockupError on empty
   - dump_mockup round-trips (Notes persist all fields)
   - validate_mockup checks register bounds via muse_assert chain

2. **CLI**
   - <work> → writes .mockup.json
   - Note count matches pitched-note count (excludes rests, unpitched)

## How to run

```bash
python3 tools/muse_mockup/cli.py corpus/bach/bwv227.1.mxl
```
