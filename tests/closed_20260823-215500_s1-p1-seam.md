# Test spec — S1 → P1 seam (issue #168)

Written 2026-08-23 by the completing agent, per TASK_WORKFLOW §6.
Code under test: `tools/s1_stream/tests/test_s1_p1_seam.py` + the
`DECODER` seam constant.

## How to invoke

```bash
cd tools/s1_stream && python3 -m pytest tests/test_s1_p1_seam.py -q
```

## Coverage landed with the task

- **Round-trip contract (4 corpus tiers):** S1 golden vector ↔ decoder
  output — meta, maps, part/note counts, and every note's (pitch, onset,
  duration) compared exactly.
- **Boundary loudness:** non-roll bytes and empty input raise at the
  decoder boundary, never partial data.
- **Vector-set coverage:** the golden directory must keep covering every
  registry tier (a new corpus work without a vector is a seam gap).

## The swap contract

`DECODER = roll_decode  # P1-stub` — when P1 (sandboxed reference decoder)
lands, swapping that one line runs every test here against the real
decoder unchanged. The chain's `decode(P1-stub)` stage pin (issue #171)
fires on the same day.

## Behaviors still needing coverage (follow-up)

- **Real-P1 run:** the seam is verified against the stub today; the first
  P1 run must be captured in the issue (with output evidence).
- **P1 sandbox bounds:** step/memory caps are P1's own DoD; the seam only
  checks I/O shape. If P1's decoder exposes resource stats, the seam test
  should pin their presence.
- **Divergent-byte diff:** on mismatch the test names the first divergent
  note; a full byte-level diff belongs to P1's own diagnostics.
