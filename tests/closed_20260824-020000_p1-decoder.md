# Test spec — P1 reference decoder — CLOSED

**Task:** #P1 (deterministic player — decoder)
**Written:** 2026-08-24

## What to verify

1. **Container decode**
   - `.mu` (manifest + roll) → Work with original parts + notes
   - Round-trip: pack → container → decode → exact original

2. **Failure paths**
   - Missing container → DecodeError
   - Bad zip → DecodeError
   - Missing manifest or roll → DecodeError

## How to run

```bash
python3 tools/muse_decode/cli.py file.mu
```

**Resolution (2026-08-24, run=20260823-2312-h8pk, #202):** 8 tests in
`tools/muse_decode/tests/test_decode.py` — container round-trips on both
parse paths (mxl + mid) with canonical-exact compare via `_canonical`;
all failure paths (missing container, bad zip, missing manifest, missing
roll) raise `DecodeError` or `pytest.raises`; corrupt roll member raises
with the match string. Registered in the runner fast tier
(`muse_decode=muse_decode/tests`).
