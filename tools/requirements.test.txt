# Unified test runner deps (tools/run_tests.sh). Runtime deps for every
# suite live here so one install command supports one runner call.
pytest>=8
mido>=1.3
pyyaml
matplotlib
playwright  # qa_frontend suite (browser itself installs full-tier-only, see run_tests.sh)
