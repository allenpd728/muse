{
  "name": "muse",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Executable music format — symbolic parsers (compressor input stage)",
  "scripts": {
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "@tonejs/midi": "^2.0.28",
    "@xmldom/xmldom": "^0.8.15",
    "fflate": "^0.8.3",
    "midi-file": "^1.2.4",
    "musicxml-interfaces": "^0.0.21"
  }
}
